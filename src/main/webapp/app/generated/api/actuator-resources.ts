/**
 * OpenAPI definition
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * API Version: v0
 * 
 *
 * NOTE: This file is auto-generated. Do not edit manually.
 */

/**
 * ActuatorApi Resources - Signal-based HTTP resources for GET operations
 * Uses Angular's httpResource for reactive data fetching
 * @generated from OpenAPI specification
 */
import { httpResource, HttpResourceRef } from '@angular/common/http';
import { Signal } from '@angular/core';
import { Link } from '../model/link';

const BASE_PATH = '';

/**
 * Actuator web endpoint \&#39;jhimetrics\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 */
export function allMetricsResource(): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        return `${BASE_PATH}/management/jhimetrics`;
    });
}

/**
 * Actuator web endpoint \&#39;jhiopenapigroups\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 */
export function allOpenApiResource(): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        return `${BASE_PATH}/management/jhiopenapigroups`;
    });
}

/**
 * Query parameters for cache
 */
export interface CacheParams {
    cacheManager?: string;
}

/**
 * Actuator web endpoint \&#39;caches-cache\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 * @param cache 
 * @param params Optional signal containing query parameters
 */
export function cacheResource(cache: Signal<string> | string, params?: Signal<CacheParams>): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        const cacheValue = typeof cache === 'function' ? cache() : cache;
        const cachePath = encodeURIComponent(String(cacheValue));
        const queryParams = params?.() ?? {};
        const searchParams = new URLSearchParams();
        if (queryParams.cacheManager !== undefined && queryParams.cacheManager !== null) {
            searchParams.set('cacheManager', String(queryParams.cacheManager));
        }
        const query = searchParams.toString();
        return `${BASE_PATH}/management/caches/${cachePath}${query ? `?${query}` : ''}`;
    });
}

/**
 * Actuator web endpoint \&#39;caches\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 */
export function cachesResource(): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        return `${BASE_PATH}/management/caches`;
    });
}

/**
 * Actuator web endpoint \&#39;configprops\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 */
export function configurationPropertiesResource(): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        return `${BASE_PATH}/management/configprops`;
    });
}

/**
 * Actuator web endpoint \&#39;configprops-prefix\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 * @param prefix 
 */
export function configurationPropertiesWithPrefixResource(prefix: Signal<string> | string): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        const prefixValue = typeof prefix === 'function' ? prefix() : prefix;
        const prefixPath = encodeURIComponent(String(prefixValue));
        return `${BASE_PATH}/management/configprops/${prefixPath}`;
    });
}

/**
 * Query parameters for environment
 */
export interface EnvironmentParams {
    pattern?: string;
}

/**
 * Actuator web endpoint \&#39;env\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 * @param params Optional signal containing query parameters
 */
export function environmentResource(params?: Signal<EnvironmentParams>): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        const queryParams = params?.() ?? {};
        const searchParams = new URLSearchParams();
        if (queryParams.pattern !== undefined && queryParams.pattern !== null) {
            searchParams.set('pattern', String(queryParams.pattern));
        }
        const query = searchParams.toString();
        return `${BASE_PATH}/management/env${query ? `?${query}` : ''}`;
    });
}

/**
 * Actuator web endpoint \&#39;env-toMatch\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 * @param toMatch 
 */
export function environmentEntryResource(toMatch: Signal<string> | string): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        const toMatchValue = typeof toMatch === 'function' ? toMatch() : toMatch;
        const toMatchPath = encodeURIComponent(String(toMatchValue));
        return `${BASE_PATH}/management/env/${toMatchPath}`;
    });
}

/**
 * Actuator web endpoint \&#39;health\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 */
export function healthResource(): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        return `${BASE_PATH}/management/health`;
    });
}

/**
 * Actuator web endpoint \&#39;info\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 */
export function infoResource(): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        return `${BASE_PATH}/management/info`;
    });
}

/**
 * Actuator root web endpoint
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 */
export function linksResource(): HttpResourceRef<{ [key: string]: { [key: string]: Link; }; } | undefined> {
    return httpResource<{ [key: string]: { [key: string]: Link; }; }>(() => {
        return `${BASE_PATH}/management`;
    });
}

/**
 * Actuator web endpoint \&#39;liquibase\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 */
export function liquibaseBeansResource(): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        return `${BASE_PATH}/management/liquibase`;
    });
}

/**
 * Actuator web endpoint \&#39;loggers-name\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 * @param name 
 */
export function loggerLevelsResource(name: Signal<string> | string): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        const nameValue = typeof name === 'function' ? name() : name;
        const namePath = encodeURIComponent(String(nameValue));
        return `${BASE_PATH}/management/loggers/${namePath}`;
    });
}

/**
 * Actuator web endpoint \&#39;loggers\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 */
export function loggersResource(): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        return `${BASE_PATH}/management/loggers`;
    });
}

/**
 * Actuator web endpoint \&#39;threaddump\&#39;
 * 
 * Creates a reactive HTTP resource that automatically refetches when signals change.
 */
export function threadDumpResource(): HttpResourceRef<object | undefined> {
    return httpResource<object>(() => {
        return `${BASE_PATH}/management/threaddump`;
    });
}

